services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: babyhands
      POSTGRES_USER: test
      POSTGRES_PASSWORD: test
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
